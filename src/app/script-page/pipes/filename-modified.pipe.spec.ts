import { FilenameModifiedPipe } from './filename-modified.pipe';

describe('FilenameModifiedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilenameModifiedPipe();
    expect(pipe).toBeTruthy();
  });
});
