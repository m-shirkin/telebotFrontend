import { ArrayEmptyPipe } from './array-empty.pipe';

describe('ArrayEmptyPipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayEmptyPipe();
    expect(pipe).toBeTruthy();
  });
});
